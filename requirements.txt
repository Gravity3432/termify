spotipy>=2.23
librespot>=0.0.9
av>=12
sounddevice>=0.4
rich>=13
readchar>=4
Pillow>=10
requests>=2.28
numpy>=1.24
# optional: enables global keyboard media buttons (play/pause/next/prev)
# even when the terminal isn't focused. Remove this line if it fails to
# install on your system - Termify still works, just without those keys.
keyboard>=0.13.5
python-sixel-windows>=0.2.2 ; platform_system=='Windows'
